/**
* This object represents the Browser toolbar. 
* The interface it provides allows for adding, removing and editing <code>ToolbarItems</code> attached to it.
* @final
* @type ToolbarContext
**/
Contexts.prototype.toolbar = new ToolBarContext();

/**
* This object represents the Browser's in-page right-click menu.
* The interface it provides allows for adding, removing and editing <code>MenuUIItems</code> attached to it.
* @final
* @type MenuContext
**/
Contexts.prototype.menu = new MenuContext();